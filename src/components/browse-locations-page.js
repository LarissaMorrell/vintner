import React from 'react';
import {connect} from 'react-redux';
import {Redirect} from 'react-router-dom';

export default class BrowseLocationsPage extends React.Component {

    render() {
          return (
            <div>Browse Locations Page</div>
          );
    }
}
